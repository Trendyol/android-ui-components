<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.trendyol.uicomponents">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RatingBarActivity" />
        <activity android:name=".DialogsActivity"
            android:windowSoftInputMode="adjustResize">
        </activity>
        <activity android:name=".ImageSliderActivity" />
        <activity android:name=".PhoneNumberActivity" />
        <activity android:name=".ToolbarActivity" />
        <activity android:name=".SuggestionInputViewActivity" />
        <activity android:name=".CardInputViewActivity" />
        <activity android:name=".QuantityPickerViewActivity" />
        <activity android:name=".TimelineViewActivity"/>
        <activity android:name=".FitOptionMessageViewActivity"/>

    </application>

</manifest>